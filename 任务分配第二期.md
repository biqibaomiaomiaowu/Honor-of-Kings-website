## 要求：任务完成过后在后面标注（已完成）
## 这是第二版任务，要求在 12.15 前完成

**主要目标**：将现有的静态 HTML 网站迁移至 Vue 3 + Vite 工程化项目，实现组件化开发和路由管理。

---

####  迁移开发指南
*   **语法标准**：统一使用 Vue 3 `<script setup>` 语法。
*   **样式迁移**：原 HTML 中的 CSS 放入组件的 `<style scoped>` 中。
*   **资源引用**：图片路径从 `./imgs/xxx` 修改为 `/imgs/xxx`。
*   **根元素规则**：虽然 Vue 3 支持多根节点，但为了兼容性和避免 ESLint 报错（如 `[vue/no-multiple-template-root]`），建议每个组件的 `<template>` 最外层仍保留一个 `<div>` 包裹。

---

### 👥 详细任务分配

#### 公共部分 (曹乐完成)
*   [ ] 项目初始化与依赖安装
*   [ ] `Navbar.vue` 组件封装
*   [ ] `App.vue` 布局配置 (引入 Navbar 和 RouterView)
*   [ ] 路由表基础配置 (`src/router/index.js`)

#### 朱春雯：
*   [ ] **HeroesView.vue**: 迁移 `heroes.html` 的逻辑与样式。
*   [ ] **WorldView.vue**: 迁移 `world.html` 的逻辑与样式。

#### 胡皓宇：
*   [ ] **EquipmentView.vue**: 迁移 `equipment.html` 的逻辑与样式。
#### 曹乐：
*   [ ] **HomeView.vue**: 迁移 `index.html` 的逻辑与样式。
*   [ ] **MapsView.vue**: 迁移 `maps.html` 的逻辑与样式。